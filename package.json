{
  "name": "expertisemarkt",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "test:em": "node scripts/em-integration-test.mjs",
    "test:em:full": "node scripts/em-full-flow-test.mjs",
    "test:em:agent": "node scripts/em-user-agent-test.mjs",
    "electron": "npm run build && npm --prefix electron install && npm --prefix electron start",
    "dist": "npm run build && npm --prefix electron install && npm --prefix electron run dist"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "vite": "^8.1.1"
  }
}
